// Wait for the document to be ready
$(document).ready(function () {
  // Use the jQuery selector to select the <header> element
  // Then use the `.css()` method to set the text color to red (#FF0000)
  $('header').css('color', '#FF0000');
});
